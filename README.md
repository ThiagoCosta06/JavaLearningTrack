# JavaLearningTracking
This repositorie will be used to track Java learning curve, so that the progress can be tracked through github.


- This project uses the H2 database(url: localhost:8080/h2-console)
    User: admin
    Password: admin
- To start the project used the play button on the IDE.